{
  "name": "fixture-node-cli",
  "private": true,
  "version": "0.0.0",
  "bin": {
    "fixture-cli": "src/cli.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
